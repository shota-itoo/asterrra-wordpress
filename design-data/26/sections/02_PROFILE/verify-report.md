# verify-report: PROFILE (会社概要テーブル)

- **対象**: ページID 26 / セクション `.sec-profile`（2番目・append 挿入）
- **環境**: コンテナ `wp-env-asterrra-wordpress-b07ac4e0-cli-1` / ポート 8918 / セッション asterrra-company / 検証幅 1440px
- **検証日**: 2026-06-16
- **入力**: `02_PROFILE/tree.js`（25,907 bytes へシリアライズ）

## 結果

**合格（PASS）**。tree.js を実機シリアライズ→ページ26へ append 挿入し、isValid 全件通過・計測突合も幾何一致（ラウンド0で収束、修正ループ0回）。既存 HERO セクションは未改変。重点確認2点（ネイビー背景の行全高被覆／セル下罫線のUB border属性出力）はいずれも実機で確認。

## isValid

- 挿入後 post_content: 33,765 bytes（HERO 6,908 + PROFILE 25,907 + 区切り）
- ブロックエディタ再読込での検証: **total 77 / allValid:true / invalid:[]**
- ラベルセル背景 #131C30、白20%罫線、UB固定幅・stretch すべてバリデーション通過

## 計測突合表（front-end `.sec-profile` / viewport 1440×900）

| 項目 | 設計値（datapack） | 実測 | 判定 |
|---|---|---|---|
| セクション幅 | 1440（full-wide） | 1440 | ✓ |
| テーブル幅 | 896px | 896px | ✓ |
| テーブル maxWidth | 896px | 896px | ✓ |
| テーブル中央寄せ | 左右マージン 272px | margin-left/right 272px | ✓ |
| テーブル末尾下罫線 | 1px #E5E7EB | 1px rgb(229,231,235) | ✓ |
| 行間 gap | 0（罫線区切り） | 行が密着・罫線で区切り | ✓ |
| 行数 | 10 | 10 | ✓ |
| 箇条書きリスト数 | 2 | 2（ul×2・各4項目） | ✓ |
| ラベル列幅（全行） | 256px | 256px（10/10行） | ✓ |
| 値列幅（全行） | 640px | 640px（10/10行） | ✓ |
| ラベルセル背景 | #131C30（ネイビー） | rgb(19,28,48)（10/10行） | ✓ |
| **ラベル背景が行全高を被覆** | 行UB stretch でセル同高 | **sameH:true（10/10行）labH=valH=rowH** | ✓ |
| **値セル下罫線（UB border属性出力）** | 1px #E5E7EB（1〜9行） | 1px rgb(229,231,235)（1〜9行） | ✓ |
| **ラベルセル下罫線（UB border属性出力）** | 1px rgba(255,255,255,0.2)（1〜9行） | 1px rgba(255,255,255,0.2)（1〜9行） | ✓ |
| FAX行（最終）罫線 | なし | 0px（ラベル/値とも・末尾全体罫線でカバー） | ✓ |
| 標準行 高さ | 72/73（生73） | 73px（i=0〜5,8） | ✓ |
| リスト行 高さ | 208（生207.5） | 181px（i=6,7） | △ 許容差（後述） |
| リスト各行 項目数 | 4 | 4（i=6,7とも） | ✓ |

### 重点確認2点の結論
- **① ネイビー背景が行全高を覆うか** → ◯。行UB `layoutAlign:'stretch'` が効き、全10行でラベルセル高さ＝値セル高さ＝行高（sameH:true）。ネイビーが行全体を覆っている。
- **② セル下罫線が UB border 属性でフロント出力されるか** → ◯。値セル 1px #E5E7EB・ラベルセル 1px rgba(255,255,255,0.2) が `style.border.bottom` から実フロントに出力されている。**CSSフォールバック不要**（残課題に「要CSS: テーブル罫線」は計上しない）。

## 修正履歴

- ラウンド0で全項目収束。tree.js 修正・属性修正は**実施せず**（修正ループ 0/3 回）。

## 残課題

### CSS待ち（既知）
1. **リストマーカー色 #C8A97E** — core/list の `::marker` 色はブロック属性で指定不可。実機ではデフォルトの濃色ドットで表示中。後段ページCSSで `.sec-profile .asterrra-dotlist li::marker { color:#C8A97E; }`（または 4×4px 小丸を擬似要素で）を適用予定。**本検証では不一致判定しない（datapack §要CSS と整合）。**
2. **テーブルの軽い影 `0 1px 2px 0 rgba(0,0,0,0.05)`** — UB box-shadow extension はカスタム値で未生成のことがあり、属性で出ていない。装飾レイヤー（軽い浮き）のため未適用でも崩れなし。後段ページCSSで `.sec-profile [テーブル枠]{ box-shadow:... }` を適用予定。**本検証では不一致判定しない。**

### 注記（不一致ではない許容差）
- **リスト行高 181px（設計正規値208・生207.5）**: 差は core/list の blockGap(15.5px) とデフォルト行マージンの差に起因。レイアウト整合・列幅・stretch 被覆・項目数はすべて正常で、テーブル骨格に破綻なし。重点確認外のため許容。マーカー実装（要CSS）でリスト内余白を微調整する際に併せて詰めるのが適切。

### 触れていないもの（仕様通り）
- HERO セクション（1番目 `loos/full-wide` #131C30）は未改変。
- ページ26メタ `swell_meta_show_sidebar=hide` は変更なし（確認済み: hide）。
- テキストは tree.js のまま（一字一句変更なし）。

## 成果物
- 実装スクショ: `02_PROFILE/implemented.png`（フルページ・1440幅）
- 挿入後ページ: ID26 post_content（HERO + PROFILE）
