# verify-report: Contact Form Section

- **対象**: ページID `29`（お問い合わせ / slug `/contact/`）/ セクション `.sec-form`（02_FORM）
- **挿入モード**: `append`（既存 `.sec-hero` の下に追加。Hero ブロックには非接触）
- **環境**: コンテナ `wp-env-asterrra-wordpress-b07ac4e0-cli-1` / ポート `8918` / セッション `asterrra-c` / デザイン幅 `1440`（内コンテンツ `1024`）
- **検証日**: 2026-06-16

## 結果

**合格（PASS / 1ラウンドで収束）**

- ページ全体ブロック isValid = **allValid: true**（39 ブロック中 invalid 0）
- `.sec-form` 2カラム（左=白フォームカード / 右=ネイビー連絡先カード）・リード文・右カード連絡先テキストの幾何/色/タイポすべて設計値と一致
- SMF 埋め込みブロック（formId 63）が実 `<form>` をレンダリングし、入力欄 **12 個**（閾値 6 を大きく超過）

## isValid

| 項目 | 値 |
|---|---|
| 総ブロック数（ページ全体） | 39 |
| allValid | **true** |
| invalid ブロック | なし（[]） |
| SMF dynamic block | `<!-- wp:snow-monkey-forms/snow-monkey-form {"formId":63} /-->` で valid。エディタで正常解決（プラグイン snow-monkey-forms 12.1.3 active） |

## 計測突合表（`.sec-form` / フロント `/contact/?dev_login=1` / viewport 1440×900）

| 要素 | 設計値 | 実測値 | 判定 |
|---|---|---|---|
| セクション `.sec-form` 背景 | #ffffff（白） | rgb(255,255,255) | ✓ |
| セクション全幅（alignfull） | viewport 全幅 | w=1440（x=0, y=848＝Hero下に配置） | ✓ |
| 内コンテンツ行幅（2カラム本体 row） | 1024 | 1024 | ✓ |
| リード文（中央寄せ p） | 存在 | lead: true（段落描画） | ✓ |
| **左フォームカード 幅** | 660px | w=660（x=208） | ✓ |
| 左カード 背景 | #f8f9fa | rgb(248,249,250) | ✓ |
| 左カード padding | 64px 全方向 | 64px | ✓ |
| **右ネイビーカード 幅** | 331px | w=331（x=900） | ✓ |
| 右カード 背景 | #131C30 | rgb(19,28,48) | ✓ |
| 右カード padding | 48px 全方向 | 48px | ✓ |
| カラム gap | 32px | 32px（900 −（208+660）） | ✓ |
| 左右カード 高さ揃い（self-stretch相当） | 同高 | 左811 / 右811（同高） | ✓ |
| **フォーム描画** | `<form>` 存在 | formExists: true | ✓ |
| **フォーム入力欄数** | ≥ 6 | **12**（input/select/textarea） | ✓ |
| 右カード連絡先テキスト | 「お電話でのお問い合わせ」「会社情報」含む | navyCardText: true | ✓ |
| 電話番号テキスト | 03 6912 0000 | "03 6912 0000" | ✓ |
| 電話番号 font-size | 30px | 30px | ✓ |
| 電話番号 色（ゴールド） | #C8A97E | rgb(200,169,126) | ✓ |
| H3「会社情報」 font-size | 16px | 16px | ✓ |
| H3「会社情報」 色 | #ffffff | rgb(255,255,255) | ✓ |

> 視覚確認（implemented.png）: Hero（お問い合わせバナー）下にフォームセクションが正しく配置。左カードに SMF が実フォーム（お名前 / フリガナ / メールアドレス / 電話番号 / お問い合わせ種別 select / お問い合わせ内容 textarea / プライバシー同意チェック / 送信ボタン）を描画。右ネイビーカードにゴールド電話番号・受付時間・区切り線・会社情報・住所・TEL/FAX を表示。

## 修正履歴

なし（ラウンド 1 で isValid・計測ともに収束。tree.js 無修正）。

## 残課題

- **CSS待ち（既知）**:
  - 左フォームカードの弱い影 `box-shadow: 0 1px 1px rgba(0,0,0,0.05)`（属性不可）
  - 右ネイビーカードのドロップシャドウ `box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -4px rgba(0,0,0,0.1)`（属性不可）
  - SMF フォーム内部スタイル（入力欄 border 1px #e5e7eb・必須バッジ ゴールド #c8a97e・送信ボタン ゴールド #c8a97e・ラベル列幅・プレースホルダ色等）→ SMF は既定スタイルで素のまま描画されており正常。後段ページCSSで適用予定。
  - いずれも見た目未適用でも本検証では不一致と判定しない方針どおり（幾何・テキスト・色トークンは一致済み）。
- デザイン上のダミー値（電話/FAX/郵便番号/住所＝000-0000 等）と `tel:` リンク化要否は datapack 6章のとおり要ユーザー確認（本タスクのスコープ外・テキスト変更禁止）。
