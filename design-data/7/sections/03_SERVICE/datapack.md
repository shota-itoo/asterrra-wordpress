# データパック: SERVICE（事業内容）セクション

- **Figma file**: `eZFYBaJDAN0PLLd7ITdvLR`
- **nodeId**: `1:930`（Section - SERVICE）
- **デザイン全幅**: 1440px（セクション 1440×881）
- **設計**: ダークネイビー背景に4枚のサービスカードを横並び。各カードは「上部に画像（254×162）＋下部にネイビーカード本体（254×179、見出し・説明文・装飾アイコン）」の構成。最下部にCTAボタン。
- **design.png**: `./design.png`（1440×881）

---

## 1. レイアウト骨格

### セクション全体（`1:930`）
- 背景色: `#0a192f`（ネイビー）
- セクション寸法: 1440 × 881（デザイン幅基準）
- padding: 上 106px / 下 128px / 左右 144px
- 内側は縦1カラム。中央寄せのコンテンツコンテナを持つ。

### コンテナ（`1:931`）
- x=144, width=1152（= デザイン幅1440 − 左右マージン144×2）
- さらに内側 padding-x = 32px → 実コンテンツ幅 = **1088px**
- 子要素は縦並び、**gap = 40px**
- 子の順序: ①見出しブロック → ②導入テキスト → ③カードグリッド → ④CTAボタン

### ①見出しブロック（`1:932`）
- 縦並び、gap = 8px、幅 1088（中央寄せの子）
- 子: 「SERVICE」（大見出し・中央）／「事業内容」（小ラベル・中央）

### ②導入テキスト（`1:937`）
- 中央寄せ、2行（強制改行あり）

### ③カードグリッド（`1:939`）
- width = **1088px**, height ≈ 339px
- **4カラム横並び、各カード幅 254px、gap = 24px**
  - 検算: 254×4 + 24×3 = 1016 + 72 = 1088 ✓
- カラム左端 x座標（グリッド内）: card1=0 / card2=278 / card3=556 / card4=834（278 = 254+24 ✓）
- **各カードの内部構成（重要）**:
  - 上部: 画像（254 × 約162px、y=0〜161.94）
  - 下部: ネイビーのカード本体（254 × 約179px、y≈160〜339）
  - 画像下端(161.94)とカード本体上端(160.06)が **約1.9px だけ重なる**＝画像の下にカード本体が地続きで接する見え方（隙間なし）。実装上は「画像ブロックの直下にカード本体ブロックを隙間ゼロで積む」でよい。

### ④CTAボタン（`1:992` / `1:993`）
- 親コンテナ `1:992`: 中央寄せ、padding-top = 24px
- ボタン本体 `1:993`: 262 × 52px、中央配置

---

## 2. テキスト（一字一句／⏎=`<br>`）

### 見出しブロック
- 大見出し（`1:934`）: `SERVICE`
- 小ラベル（`1:936`）: `事業内容`

### 導入テキスト（`1:938`）2行・強制改行
```
目先の利益や取引だけを追求するのではなく、その先にあるお客様の未来を見据え、⏎
長期的な視点で価値あるサポートを提供いたします。
```

### カード1: 不動産売買サポート（カラム `1:940`）
- カード見出し（`1:944`）: `不動産売買サポート`（1行）
- 説明文（`1:946`）3行・強制改行:
```
居住用不動産から投資用不動産まで、⏎
お客様のご希望に応じた⏎
最適なご提案を行います。
```

### カード2: 賃貸・管理サポート（カラム `1:951`）
- カード見出し（`1:957`）: `賃貸・管理サポート`（1行）
- 説明文（`1:959`）2行・強制改行:
```
賃貸の仲介から管理業務まで⏎
幅広くサポートいたします。
```

### カード3: リフォーム リノベーション（カラム `1:978`）
- カード見出し（`1:985`）2行・強制改行（中黒や「・」は無し、改行で2段）:
```
リフォーム⏎
リノベーション
```
- 説明文（`1:987`）3行・強制改行:
```
新たな価値を創造し、⏎
未来につながる住まいづくりを⏎
ご提案します。
```

### カード4: 不動産コンサルティング（カラム `1:963`）
- カード見出し（`1:972`）2行・強制改行:
```
不動産⏎
コンサルティング
```
- 説明文（`1:974`）3行・強制改行:
```
市場分析や相続対策など、⏎
幅広いご相談に⏎
対応いたします。
```

### CTAボタン（`1:993`）
- ラベル（`I1:993;1:705`）: `事業内容一覧を見る`
- アイコン（`I1:993;1:707`）: `→`（テキストの矢印グリフ。ラベルとの gap = 16px）

---

## 3. スタイル

### カラーパレット（このセクションで使用）
| 用途 | HEX | 備考 |
|---|---|---|
| セクション背景 | `#0a192f` | ネイビー（ページ標準ネイビー） |
| カード本体背景 / CTAボタン背景 | `#1e2c5b` | やや明るいネイビー |
| 大見出し SERVICE | `#c5a059` | ゴールド（Figma変数名 Twine / orange-56。※ページ標準ゴールド #c2a24c とは別値。本セクションは #c5a059 が実値） |
| 本文・見出し・ラベル・ボタン文字・アイコン | `#ffffff` | 白 |
| カード境界線 / 画像枠線 | `#d9d9d9` | 1px solid（カード本体＋画像に付与） |
| セクション装飾枠（外周） | `#989898` | 1px solid（装飾レイヤー、§4参照） |

### タイポグラフィ
| 要素 | フォント | size | weight | line-height | letter-spacing | align | color |
|---|---|---|---|---|---|---|---|
| 大見出し SERVICE (`1:934`) | Noto Serif JP SemiBold | 36px | 600 | 40px | 3.6px | center | #c5a059 |
| 小ラベル 事業内容 (`1:936`) | Noto Sans JP Regular | 12px | 400 | 16px | 1.2px | center | #ffffff |
| 導入テキスト (`1:938`) | Noto Serif JP Regular | 16px | 400 | 24px | （未指定/0） | center | #ffffff |
| カード見出し (`1:944` 他) | Noto Sans JP Bold | 14px | 700 | 20px | 1.4px | center | #ffffff |
| カード説明文 (`1:946` 他) | Noto Sans JP Regular | 12px | 400 | 19.5px | （未指定/0） | center | #ffffff |
| CTAラベル (`1:705`) | Noto Sans JP Regular | 14px | 400 | 20px | 1.4px | center | #ffffff |
| CTA矢印 → (`1:707`) | Noto Sans JP Regular | 12px | 400 | 16px | 1.4px | center | #ffffff |

> 注: 導入テキストは **Noto Serif JP**（明朝）、カード内テキストは **Noto Sans JP**（ゴシック）。大見出しは Noto Serif JP SemiBold。

### カード本体ボックス（`1:941`/`1:954`/`1:969`/`1:982`）
- 背景: `#1e2c5b`
- border: 1px solid `#d9d9d9`
- drop-shadow: `0px 4px 10px rgba(0,0,0,0.05)`
- padding: 33px（全方向）
- width: 254px, height: ≈179px
- 内部レイアウト: 縦並び・中央寄せ（items-center）
  - 見出しブロック（高さ56px、内 Heading 自体は高さ40px、下に padding-bottom 16px）
  - 説明文コンテナ（中央寄せ）
- 装飾アイコンはカード右上に絶対配置（§4参照）

### カード上部画像（`1:950`/`1:952`/`1:991`/`1:964`）
- width: 254px, height: ≈162px
- object-fit: cover
- border: 1px solid `#d9d9d9`

### CTAボタン（`1:993`）
- 背景: `#1e2c5b`
- padding: 上下 16px / 左右 48px
- 中身: ラベル `事業内容一覧を見る` + `→`、横並び・中央寄せ・gap 16px
- 角丸・border の指定なし（矩形）
- 実寸: 262 × 52px

---

## 4. 装飾レイヤー

### 4-1. カード内アイコン（各カード右上に絶対配置・白色）
全アイコンは白（`#ffffff`）のマスク図形で、カード本体の **右上** に絶対配置される。座標はカード本体（254×179）の左上原点基準（カード本体ローカル座標／padding 33 の外側に張り出す）。

| カード | アイコンフレーム | 内容（モチーフ） | サイズ(px) | カード内 left(px) | カード内 top(px) |
|---|---|---|---|---|---|
| 1 不動産売買サポート | `1:947` | 家＋きらめき（house + sparkle） | 47 × 49.3 | 182 | 24 |
| 2 賃貸・管理サポート | `1:960` | 書類/チェックリスト＋シールド（盾チェック） | 41 × 44.3 | 185 | 29.7 |
| 3 リフォーム リノベーション | `1:988` | ビル群＋新芽（葉） | 51 × 58 | 170 | 22.7 |
| 4 不動産コンサルティング | `1:975` | 鍵を持つ手 | 51.4 × 41.8 | 181 | 29.7 |

- 配置の意味: 各カードの右上隅近く（カード幅254に対し left≈170〜185）に、padding を無視して白アイコンを重ねる。実装は各カードに `position:relative`、アイコンを `position:absolute; top≈22〜30px; right≈25〜45px` で配置（rightは 254 − left − iconW で算出: card1≈25 / card2≈28 / card3≈33 / card4≈22）。
- 色: 単色 白。透過PNGとして取得済み（§5）。

### 4-2. セクション外周の装飾枠（`1:994` "Rectangle 46"）
- border: 1px solid `#989898`
- 寸法: 1376 × 801px
- 位置: セクション原点基準で left=32, top=41（＝セクション padding-x 144 ではなく、もっと外側の細枠。デザイン幅1440に対し左右32px余白を残した薄いグレー枠線がセクション全体を囲う装飾）
- 役割: セクション全体を囲う装飾的な細枠（コンテンツではない）。実装時は装飾レイヤーとして、セクション内に absolute の枠 or `::before` で表現（カスタムCSS要・ブロック属性のみでは表現困難な可能性 → 実装前にユーザー確認推奨）。

---

## 5. アセット（guid確認済み・WPメディア取込済み）

WPコンテナ: `wp-env-asterrra-wordpress-b07ac4e0-cli-1`（ポート 8918）。URLはDBから取得した実値（推測ではない）。

### カード上部画像（写真・実寸254×162を2x=508×324でエクスポート）
| 用途カード | nodeId | 内容 | 取得ファイル | 寸法 | WP ID | URL |
|---|---|---|---|---|---|---|
| 1 不動産売買サポート | `1:950` | モダンな会社外装ビル | `assets/service-card1-baibai.png` | 508×324 | **15** | http://localhost:8918/wp-content/uploads/2026/06/service-card1-baibai.png |
| 2 賃貸・管理サポート | `1:952` | 戸建て住宅（青空） | `assets/service-card2-chintai.png` | 508×324 | **16** | http://localhost:8918/wp-content/uploads/2026/06/service-card2-chintai.png |
| 3 リフォーム リノベーション | `1:991` | リフォーム後の室内 | `assets/service-card3-reform.png` | 508×324 | **17** | http://localhost:8918/wp-content/uploads/2026/06/service-card3-reform.png |
| 4 不動産コンサルティング | `1:964` | 打合せ/相談シーン | `assets/service-card4-consulting.png` | 508×324 | **18** | http://localhost:8918/wp-content/uploads/2026/06/service-card4-consulting.png |

### カード内アイコン（白・透過PNG、4xエクスポート）
| 用途カード | nodeId | モチーフ | 取得ファイル | 寸法 | WP ID | URL |
|---|---|---|---|---|---|---|
| 1 不動産売買サポート | `1:949` | 家＋きらめき | `assets/service-icon1-baibai.png` | 195×205 | **19** | http://localhost:8918/wp-content/uploads/2026/06/service-icon1-baibai.png |
| 2 賃貸・管理サポート | `1:962` | 書類＋盾チェック | `assets/service-icon2-chintai.png` | 180×196 | **20** | http://localhost:8918/wp-content/uploads/2026/06/service-icon2-chintai.png |
| 3 リフォーム リノベーション | `1:990` | ビル群＋新芽 | `assets/service-icon3-reform.png` | 204×232 | **21** | http://localhost:8918/wp-content/uploads/2026/06/service-icon3-reform.png |
| 4 不動産コンサルティング | `1:977` | 鍵を持つ手 | `assets/service-icon4-consulting.png` | 222×180 | **22** | http://localhost:8918/wp-content/uploads/2026/06/service-icon4-consulting.png |

> アイコンは白オン透過。`Read`ツールで白背景に開くと一見空白に見えるが、形状は確認済み（house+sparkle / 書類+盾 / ビル+葉 / 鍵を持つ手）。フロントではネイビーカード上に白で表示される。
> アイコン表示サイズ（フロント）は §4-1 の「サイズ(px)」列（47×49.3 等）を使う。取得PNGはレティナ用に高解像度。

---

## 6. インタラクション台帳

| 要素 | 種別 | 挙動 | リンク先 |
|---|---|---|---|
| CTAボタン `事業内容一覧を見る →` | リンクボタン | クリックで事業内容一覧ページへ遷移（想定） | **要ユーザー確認**（Figmaにリンク先指定なし。事業内容アーカイブ/固定ページのURLを確認） |
| 各サービスカード（4枚） | 表示のみ | Figma上ではリンク指定なし。ホバー/クリック挙動の定義なし | **要ユーザー確認**（各カードを個別サービス詳細ページへリンクさせるか／非リンクのままか） |

---

## 7. 正規化記録（生値 → 正規値、4/8pxグリッド丸め）

| 項目 | 生値(px) | 正規値(px) | 備考 |
|---|---|---|---|
| セクション padding-top | 106 | 106 | 4の倍数でないが Figma実値（=104+2 ではなく106）。スナップするなら104 or 108だが**実値106を採用**推奨 |
| セクション padding-bottom | 128 | 128 | ✓グリッド |
| セクション padding-x | 144 | 144 | ✓グリッド（ページ標準左右マージン） |
| コンテナ width | 1152 | 1152 | ✓ページ標準コンテンツ幅 |
| コンテナ内 padding-x | 32 | 32 | ✓ → 実コンテンツ幅1088 |
| 主要縦 gap（見出し/導入/グリッド/CTA間） | 40 | 40 | ✓ |
| 見出し内 gap（SERVICE↔事業内容） | 8 | 8 | ✓ |
| カードグリッド width | 1088 | 1088 | ✓ |
| カード幅 | 254 | 254 | スナップ候補 256 だが、254×4+24×3=1088 を満たすため**実値254を維持**（256にすると合計が崩れる） |
| カード間 gap | 24 | 24 | ✓ |
| カード上部画像 高さ | 161.94 | **162** | 丸め |
| カード本体 高さ | 178.94 | **179**（≈180） | 丸め。画像162と合計≈341、重なり≈2で実カラム高339 |
| カラム総高 | 338.99〜339.0 | 339 | 画像162 + カード本体179 − 重なり≈2 |
| 画像↔カード本体 重なり | 1.88 | ≈2（実装上は隙間0で接続） | 縦に地続き |
| カード本体 padding | 33 | 32（or 33維持） | スナップなら32。Figma実値33 |
| カード見出しブロック高 | 56（内Heading40 + pb16） | 56 | 見出し下マージン16 |
| CTAボタン padding | 16 / 48 | 16 / 48 | 上下16・左右48 |
| CTAボタン 実寸 | 262 × 52 | 262 × 52 | padding由来 |
| CTA上 padding-top | 24 | 24 | ✓ |
| 各種ボーダー | 1 | 1 | カード/画像=#d9d9d9、外周枠=#989898 |
| 装飾アイコン サイズ | §4-1 実値 | §4-1 実値 | サブピクセル端数あり（例 49.333）→四捨五入で 47×49 等 |

### 採用上の注記
- 大見出しゴールドは Figma実値 **#c5a059**（変数 Twine）。ページ標準の #c2a24c とは微差あり。本セクションは実値 #c5a059 を正とする。
- 導入テキストのフォントは **Noto Serif JP（明朝）** で、本文(Noto Sans)とは別。見落とし注意。
- カード見出し「リフォーム/リノベーション」「不動産/コンサルティング」は中黒「・」ではなく改行による2段。原文に「・」は含まない（賃貸・管理サポートのみ「・」が文中にある）。

---

## 検証用設計値（tree.js 突合シート）

フロント計測時はこの数値と属性JSONを突合する。座標xはカードグリッド内ローカル（左端=0）。

| 要素 | x(px) | w(px) | h(px) | gap(px) | padding(px) | fs(px) | lh(px) | ls |
|---|---|---|---|---|---|---|---|---|
| セクション(loos/full-wide) | – | full | – | – | T106/B128/LR144 | – | – | – |
| コンテナ | 中央 | 100%/max1152 | – | 40(縦) | T106/R32/B128/L32, MX auto | – | – | – |
| 見出しブロック | 中央 | 100% | – | 8 | 0 | – | – | – |
| 大見出し SERVICE | 中央 | – | 40 | – | 0 | 36 | 40 | 0.1em(3.6px) |
| 小ラベル 事業内容 | 中央 | – | 16 | – | 0 | 12 | 16 | 0.1em(1.2px) |
| 導入テキスト(2行) | 中央 | – | – | – | 0 | 16 | 24 | 0 |
| カードグリッド | 中央 | 100%(=1088実) | 339 | 24(横) | 0 | – | – | – |
| カード(×4) | 0/278/556/834 | 254 | 339 | 0(縦) | 0 | – | – | – |
| └ 上部画像 | 0 | 254 | 162 | – | 0(border1 #d9d9d9) | – | – | – |
| └ カード本体 | 0 | 254 | 179 | 16(縦) | 33全方向(border1 #d9d9d9) | – | – | – |
| └ カード見出し(level3) | 中央 | – | 20 | – | 0 | 14 | 20 | 0.1em(1.4px) |
| └ カード説明文 | 中央 | – | – | – | 0 | 12 | 19.5 | 0 |
| └ 装飾アイコン(absolute) | – | 下表 | 下表 | – | – | – | – | – |
| CTAボタン | 中央 | 262(実寸) | 52 | – | T16/R48/B16/L48 | 14 | 20 | 0.1em(1.4px) |

### 装飾アイコン absolute 配置（カード本体ローカル座標基準）

| カード | iconW | iconH | top(px) | right(px) |
|---|---|---|---|---|
| 1 不動産売買サポート | 47 | 49 | 24 | 25 |
| 2 賃貸・管理サポート | 41 | 44 | 30 | 28 |
| 3 リフォーム リノベーション | 51 | 58 | 23 | 33 |
| 4 不動産コンサルティング | 51 | 42 | 30 | 22 |

### 配色
- セクション背景 `#0a192f` / カード本体・CTA背景 `#1e2c5b` / 大見出しゴールド `#c5a059` / 文字・アイコン `#ffffff` / カード・画像ボーダー `#d9d9d9`(1px)
- 外周装飾枠 `#989898`(1376×801, 1px) は tree.js 非掲載（§要CSS）
